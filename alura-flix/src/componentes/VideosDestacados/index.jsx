import "./VideosDestacados.css"
import { useState } from "react"
const VideosDestacados = () => {
    return <section className="VideosSection">
    </section> //al llamar a la funcion cambiarMostrar del padre app.jsx se va a mostrar u ocultar el formulario al hacer click en el boton add
}

export default VideosDestacados